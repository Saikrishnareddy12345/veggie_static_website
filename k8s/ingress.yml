apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: static-ingress
  namespace: static1
  labels:
    name: myingress
spec:
  rules:
  - host: static1.saikrish.xyz
    http:
      paths:
        backend:
          service:
            name: static-svc
            port: 
              number: 80



