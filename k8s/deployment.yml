apiVersion: apps/v1
kind: Deployment
metadata:
  name: static-deployment
  namespace: static1
spec:
  selector:
    matchLabels:
      app: static-dept
  replicas: 5 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: static-dept
    spec:
      containers:
      - name: static
        image: 913443211236.dkr.ecr.us-east-1.amazonaws.com/static:1
        ports:
        - containerPort: 80